-- Given the following lines ofr code as they might appear in a source file, how would you change them to use them directly in the REPL.

half x = x / 2
square x = x * x

-- Answer:
    let half x = x / 2
    let square x = x * x
